import 'package:flutter/material.dart';

class ReportDanger extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Scaffold(
      body: Padding(
        padding: const EdgeInsets.all(20.0),
        child: Text('Report danger screen'),
      ),
    );
  }
}
